product: android/app

dependencies:
  - ../ui
  - androidx.activity:activity-compose:1.8.2
  - androidx.appcompat:appcompat:1.6.1

settings:
  android:
    namespace: "lol.calf.nala"
  compose: enabled
  junit: junit-4
